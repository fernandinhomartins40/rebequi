name: Deploy VPS (Legacy)

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Legacy workflow notice
    runs-on: ubuntu-latest

    steps:
      - name: Explain replacement workflow
        run: |
          echo "Este workflow legado foi desativado."
          echo "Use .github/workflows/deploy-production.yml para o deploy real em producao."
          echo "O pipeline atual publica a aplicacao em rebequi.com.br e www.rebequi.com.br usando a porta interna 3190."
          exit 1
